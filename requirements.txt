# AI QA Automation — Dependencies
# Install with: pip install -r requirements.txt

# Environment configuration
python-dotenv>=1.0.0

# Google Gemini AI SDK (new unified SDK, v2.x)
google-genai>=2.0.0

# MCP (Model Context Protocol) for Playwright integration
mcp>=1.0.0

# Web UI automation (MCP-driven)
playwright>=1.44.0

# HTTP requests for API exploration
requests>=2.31.0

# Test framework
pytest>=8.0.0
pytest-playwright>=0.5.0
pytest-html>=4.0.0
pytest-json-report>=1.5.0

# JUnit XML report parsing
junitparser>=3.1.0

# Async support (required by MCP)
anyio>=4.0.0
